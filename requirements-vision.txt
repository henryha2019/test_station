# Vision training / quantization (Phase 5-6). Heavy; only needed to (re)train
# or INT8-quantize the CNN. Inference at runtime uses tflite-runtime if present,
# otherwise the feature baseline in vision/synthimg.py.
-r requirements.txt
tensorflow>=2.15            # MobileNetV2 transfer learning + post-training INT8 quant
scikit-learn>=1.3           # confusion matrix / metrics helpers (optional)
