# Normalize line endings: store LF in the repo, check out LF everywhere.
# Overrides the machine's core.autocrlf so the CRLF warnings go away.
* text=auto eol=lf

# Binary assets — never normalize.
*.png  binary
*.jpg  binary
*.jpeg binary
*.bmp  binary
*.tflite binary
*.keras  binary
